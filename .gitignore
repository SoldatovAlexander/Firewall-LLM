# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
dist/
build/
.mypy_cache/
.ruff_cache/
.pytest_cache/
.coverage
htmlcov/

# Environment & secrets - NEVER commit
.env
.env.*
*.pem
*.key
# Contents of any secrets/ dir are ignored, but the directory itself is not
# excluded so documented placeholders can be re-included below.
**/secrets/*
# ...except the documented placeholder for local scrape credentials
!deploy/secrets/README.md
vault.json

# Configs with credentials
config.local.yaml

# IDE
.idea/
.vscode/

# Local-only: review inputs and helper envs, never push
/venv-metal
docs/RELEASE_CODE_REVIEW_2026-09-06.md

# OS
.DS_Store

# Deploy runtime data (per-machine compose stack, never commit)
deploy/data/
deploy/data-rust/
deploy/certs/
deploy/models/
deploy/fwllm.yaml
deploy/prometheus-data/

# Office temp files
~$*

# runtime artifacts
*.db

# ML model artifacts
*.zip
models/

# Rust
rust/target/

# training notebook - local only, not published
docs/notebooks/
