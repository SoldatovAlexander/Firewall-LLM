server:
  host: 0.0.0.0
  port: 8080

providers:
  mock:
    type: openai_compat
    base_url: http://mock-llm:8000/v1

clients:
  bench-key: bench

routing:
  default_chain: [mock]

inspectors:
  dlp:
    mode: "off"
  injection:
    mode: "off"

egress:
  mode: direct

audit:
  enabled: false
