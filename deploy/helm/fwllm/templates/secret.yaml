{{- if .Values.secret.create }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "fwllm.fullname" . }}-secrets
type: Opaque
stringData:
  OPENROUTER_API_KEY: {{ .Values.secret.openRouterApiKey | quote }}
  FWLLM_CLIENT_TOKENS: {{ .Values.secret.clientTokens | quote }}
{{- end }}
