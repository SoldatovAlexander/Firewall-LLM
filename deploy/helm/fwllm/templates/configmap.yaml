{{- $cfg := .Values.config | toYaml | nindent 4 }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "fwllm.fullname" . }}-config
  labels: {{- include "fwllm.labels" . | nindent 4 }}
data:
  fwllm.yaml: |
{{ $cfg }}
