global:
  scrape_interval: 15s

scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets: ["localhost:9090"]

  # fwllm gateway (deploy target; shows DOWN until gateway is up)
  - job_name: fwllm-gateway
    static_configs:
      - targets: ["192.168.88.101:8080"]
